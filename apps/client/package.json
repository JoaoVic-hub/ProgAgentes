{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "cap:sync": "cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.1",
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.1",
    "fake-indexeddb": "^6.2.5",
    "vite": "^6.0.5"
  }
}
